name: user_repository
description: A repository package for user authentication and profile management
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=3.1.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  # For JSON serialization
  json_annotation: ^6.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  json_serializable: ^6.7.0

flutter:
  uses-material-design: true